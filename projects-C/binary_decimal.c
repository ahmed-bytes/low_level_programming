#include <stdio.h>
/**
 * main- A program that combines a binary number to a decimal number
 * Return: 0 means ran and exited successfully
 */

int convert(int num);

int main(void)
{
	int bin_num;

	printf("Enter The Binary Number: ");
	scanf("%d", &bin_num);
	convert(bin_num);
}


int convert(int num)
{
	int rem;
	int bin = 2;

	while (num > 0)
	{
	}

	for (int i = 0; i < ){}
}
