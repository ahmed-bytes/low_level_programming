#include <stdio.h>
/**
* main- Entry point of this program that most occuring element in an array
* Return: 0 means the program ran and evited successfully
*/

int main(void)
{
  int arr[], size;

  printf("Enter the size of the numbers: ");
  scanf("%d", &size);


  return (0);
}
