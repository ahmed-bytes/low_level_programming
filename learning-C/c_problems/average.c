#include <stdio.h>

float average_num(int arr[]);

int main(void)
{
	int num [3];

	for (int i = 0; i < 3; i++)
	{
		printf("Enter number: ");
		scanf("%d", &num[3]);
	}
	printf("The Average: %f", average_num(num[]);
}

float average_num(int arr[])
{
	int sum;

	for (int i = 0; i < arr; i++)
	{
		sum = sum + arr;
	}
	
	return (arr / float 3);
}
