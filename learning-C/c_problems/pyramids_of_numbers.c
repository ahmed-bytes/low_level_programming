#include <stdio.h>
/**
 * Write a C program to make a pattern like a pyramid 
 * with a number which will repeat the number in the same row
 */

int main(void)
{
	for (int i = 0; i <= 4; i++)
	{
		if (i <= 1)
		{
			printf(" %d\n", i);
		}
		else
		{
			printf(" %d\n", i);
		}
	}
	return (0);
}
