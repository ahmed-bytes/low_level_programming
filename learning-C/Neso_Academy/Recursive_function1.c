#include <stdio.h>

int factorial(int n);

/**
* main- Entry program to get the factorial number
* factorial: A function to get the factorial of the number
* return: 0 exit successfully
*/

int main(void)
{
  int num;

  printf("Enter number to calculate its factorials: ");
  scanf("%d", &num);

  test(num);
  return (0);
}


int factorial(int n)
{
}
