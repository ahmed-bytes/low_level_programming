#include <stdio.h>
/**
 * main- Entry point of thid program that prints out a string using printf
 * Return: If 0 means it exited successfully
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
